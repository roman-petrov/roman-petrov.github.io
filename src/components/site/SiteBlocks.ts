import type { BlockClasses } from "../index";

import styles from "./SiteText.module.scss";

export const SiteBlockClasses: BlockClasses = {
  label: styles.label,
  lead: styles.lead,
  list: styles.list,
  pull: styles.pull,
};
