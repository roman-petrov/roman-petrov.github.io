import type { BlockClasses } from "../components";

import styles from "./PrintText.module.scss";

export const PrintBlockClasses: BlockClasses = {
  label: styles.label,
  lead: styles.lead,
  list: styles.list,
  pull: styles.pull,
};
